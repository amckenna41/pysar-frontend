fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
pandas>=2.0.0
numpy
scipy
scikit-learn
aaindex>=1.3.1
protpy>=1.3.0
requests>=2.25.0
# Install pySAR directly from GitHub — the 'pysar' PyPI package is JPL's unrelated InSAR tool.
# Pinned to a commit SHA (not @master) so upstream pushes can't silently change prod
# behavior; bump deliberately by updating this SHA after testing against the new commit.
git+https://github.com/amckenna41/pySAR.git@68a256b5aa3cfefbffb80b6a582d9d050c332961
# Optional — enables the PLM-embedding strategy (feature 5). Left commented so the
# default image stays lightweight; uncomment (and rebuild) to enable ESM-2 / ProtT5.
# torch>=2.2
# transformers>=4.40
# Testing
pytest>=8.0
pytest-cov>=4.0
httpx>=0.27.0