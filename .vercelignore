# Python virtual environments
.venv
venv
env

# Python dependencies and caches
__pycache__
*.pyc
*.pyo
*.pyd
*.egg-info
.eggs

# Root-level requirements — prevents Vercel Python runtime detection
requirements.txt

# Node dependencies (Vercel installs fresh during build)
node_modules
frontend/node_modules

# Build outputs
frontend/dist

# Backend source (not needed for static frontend deploy)
backend
api

# Data and model outputs
outputs
example_datasets

# Images (served from frontend/public, not root)
images

# Docker / local tooling
Dockerfile
.dockerignore

# Misc
*.log
.DS_Store
