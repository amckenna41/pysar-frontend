[pytest]
# Discover tests under tests/backend/
testpaths = tests/backend
addopts   = -v --tb=short --strict-markers
markers   =
    unit: pure-function unit tests (no I/O)
    integration: tests that hit the FastAPI app via TestClient
    slow: tests that are deliberately slow (rate-limit windows, etc.)
